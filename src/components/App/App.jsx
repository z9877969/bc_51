import TodoPage from "../TodoPage/TodoPage";

const App = () => {
  return (
    <>
      <TodoPage />
    </>
  );
};

export default App;

// state = {count: 0}
// setState({count: 25})
// setState((prevState) => {return {count: prevState.count + 20}})